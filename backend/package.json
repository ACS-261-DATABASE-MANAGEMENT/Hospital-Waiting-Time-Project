{
  "name": "hospital-wait-backend",
  "version": "1.0.0",
  "description": "Hospital Waiting Time Dashboard — REST API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:schema": "psql -U $DB_USER -d $DB_NAME -f db/schema.sql",
    "db:seed": "psql -U $DB_USER -d $DB_NAME -f db/seed.sql"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-validator": "^7.1.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
